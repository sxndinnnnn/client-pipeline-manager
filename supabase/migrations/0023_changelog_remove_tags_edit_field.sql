insert into changelog_entries (title, description, category, released_on) values
  (
    'Removed Tags field from client editing',
    'Tags can no longer be set or edited from the UI (removed from both Add Client and the Basic Details panel). Tags already set on existing clients still display.',
    'improvement',
    current_date
  );
