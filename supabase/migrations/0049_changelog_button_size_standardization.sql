insert into changelog_entries (title, description, category, released_on) values
  (
    'Standardized button sizes across the app',
    'Buttons that were slightly larger or smaller than the rest (Login, Add Client, Add Contact, the Clients search button, and the two Save Changes buttons) now match the size used everywhere else. Icon-only buttons are unaffected.',
    'improvement',
    current_date
  );
