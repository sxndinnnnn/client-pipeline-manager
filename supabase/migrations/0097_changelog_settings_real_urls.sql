insert into changelog_entries (title, description, category, released_on) values
  (
    'Real URLs for each Settings tab',
    'Settings > Plans, Industries, Pipeline Stages, and Users each now have their own address (e.g. /settings/plans) instead of sharing one URL, so a tab can be bookmarked, shared, or opened directly.',
    'improvement',
    current_date
  );
