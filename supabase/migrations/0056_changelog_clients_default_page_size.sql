insert into changelog_entries (title, description, category, released_on) values
  (
    'Changed Clients default page size to 50',
    'The Clients page now shows 50 clients per page by default instead of 10 (still adjustable via Rows Per Page).',
    'improvement',
    current_date
  );
