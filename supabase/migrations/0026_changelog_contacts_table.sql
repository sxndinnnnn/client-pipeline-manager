insert into changelog_entries (title, description, category, released_on) values
  (
    'Contacts tab redesigned as a table',
    'Contacts now show in a table (Name, Role, Email, Phone) with edit/delete icons per row, and Add Contact opens as a proper modal — same style as Add Client.',
    'improvement',
    current_date
  );
